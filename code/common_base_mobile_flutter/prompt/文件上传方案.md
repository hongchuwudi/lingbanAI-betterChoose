## 给 Trae 的提示词

---

```
请帮我用 Flutter 实现一个文件上传服务，要求同时支持 Web、Android、iOS 平台。

## 一、依赖包

在 pubspec.yaml 中添加：
- dio: ^5.4.0
- image_picker: ^1.0.0
- http_parser: ^1.0.0

## 二、功能要求

1. 创建 FileUploadService 类，提供静态方法 uploadFile
2. 支持上传图片、视频、PDF 等文件
3. 使用 dio 的 FormData 进行文件上传
4. 兼容所有平台（Web、Android、iOS）
5. 自动识别文件 MIME 类型
6. 自动携带 token 认证

## 三、核心代码结构

```dart
import 'dart:io';
import 'package:dio/dio.dart';
import 'package:image_picker/image_picker.dart';
import 'package:http_parser/http_parser.dart';

class FileUploadService {
  static final Dio _dio = Dio();

  /// 上传文件（支持 Web / Android / iOS）
  static Future<Map<String, dynamic>> uploadFile(
    XFile file,
    String bizType,
  ) async {
    // 1. 获取 token
    // 2. 创建 FormData，使用 MultipartFile.fromBytes（所有平台兼容）
    // 3. 发送 POST 请求
    // 4. 返回结果
  }
  
  /// 将 XFile 转换为 MultipartFile
  static Future<MultipartFile> _toMultipartFile(XFile file) async {
    // 读取文件字节
    // 获取 MIME 类型
    // 返回 MultipartFile.fromBytes
  }
  
  /// 根据文件名获取 MIME 类型
  static String _getMimeType(String filename) {
    // 根据扩展名返回对应的 MIME 类型
    // 图片: image/jpeg, image/png, image/gif, image/webp
    // 视频: video/mp4
    // 文档: application/pdf
    // 默认: application/octet-stream
  }
}
```

## 四、关键实现要点

1. **使用 `XFile.readAsBytes()` 读取文件**，而不是依赖文件路径，这样 Web 平台也能正常工作
2. **使用 `MultipartFile.fromBytes()`**，传入字节数组和文件名
3. **手动指定 `Content-Type`**，通过 `MediaType.parse()` 设置
4. **添加请求头**：`Authorization: Bearer ${token}`

## 五、使用示例

```dart
// 选择图片
final picker = ImagePicker();
final XFile? image = await picker.pickImage(source: ImageSource.gallery);

// 上传
final result = await FileUploadService.uploadFile(image!, 'avatar');
if (result['success']) {
  print('上传成功: ${result['data']['fileUrl']}');
} else {
  print('上传失败: ${result['message']}');
}
```

## 六、代码要求

1. 添加详细的注释
2. 错误处理完善
3. 支持所有主流文件类型
4. 代码简洁易读

请生成完整可运行的代码。
```