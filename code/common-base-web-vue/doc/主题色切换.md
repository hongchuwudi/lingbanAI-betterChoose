DaisyUI 切换主题非常简单！我已经在之前的配置中实现了。让我详细解释几种切换方式：

## 方式1：通过 HTML 属性切换（当前已实现）

在你的 `tailwind.config.js` 中已经配置了5个主题，切换只需要修改 HTML 的 `data-theme` 属性：

```javascript
// 切换主题
document.documentElement.setAttribute('data-theme', 'light')  // 简约白
document.documentElement.setAttribute('data-theme', 'dark')   // 高级黑
document.documentElement.setAttribute('data-theme', 'pink')   // 少女粉
document.documentElement.setAttribute('data-theme', 'green')  // 清新绿
document.documentElement.setAttribute('data-theme', 'blue')   // 海洋蓝
```

## 方式2：使用 ThemeSwitcher 组件（已创建）

我之前创建的 `ThemeSwitcher.vue` 组件已经实现了主题切换功能：

```vue
<!-- src/components/ThemeSwitcher.vue -->
<template>
  <div class="dropdown dropdown-end">
    <div tabindex="0" role="button" class="btn btn-ghost btn-circle">
      <Icon :icon="SunnyIcon" theme="outline" size="20" />
    </div>
    <ul tabindex="0" class="dropdown-content z-[1] menu p-2 shadow-2xl bg-base-100 rounded-box w-52">
      <li v-for="theme in themes" :key="theme.id">
        <button @click="setTheme(theme.id)" class="flex items-center gap-2">
          <div class="w-4 h-4 rounded-full" :style="{ backgroundColor: theme.color }"></div>
          {{ theme.name }}
        </button>
      </li>
    </ul>
  </div>
</template>

<script setup>
import { ref, onMounted } from 'vue'

const themes = [
  { id: 'light', name: '简约白', color: '#3b82f6' },
  { id: 'dark', name: '高级黑', color: '#6366f1' },
  { id: 'pink', name: '少女粉', color: '#ec489a' },
  { id: 'green', name: '清新绿', color: '#10b981' },
  { id: 'blue', name: '海洋蓝', color: '#3b82f6' },
]

const setTheme = (theme) => {
  document.documentElement.setAttribute('data-theme', theme)
  localStorage.setItem('theme', theme)
}

onMounted(() => {
  const savedTheme = localStorage.getItem('theme') || 'light'
  setTheme(savedTheme)
})
</script>
```

## 方式3：创建组合式函数（推荐）

创建一个可复用的主题管理 Hook：

**`src/composables/useTheme.js`**

```javascript
import { ref, watch, onMounted } from 'vue'

// 主题列表
export const themes = [
  { id: 'light', name: '简约白', icon: '☀️', color: '#3b82f6', description: '清新明亮，适合日间使用' },
  { id: 'dark', name: '高级黑', icon: '🌙', color: '#6366f1', description: '深邃护眼，适合夜间使用' },
  { id: 'pink', name: '少女粉', icon: '🌸', color: '#ec489a', description: '温柔甜美，少女心满满' },
  { id: 'green', name: '清新绿', icon: '🌿', color: '#10b981', description: '自然舒适，护眼清新' },
  { id: 'blue', name: '海洋蓝', icon: '🌊', color: '#3b82f6', description: '宁静致远，专业稳重' },
]

export function useTheme() {
  const currentTheme = ref('light')
  
  // 切换主题
  const setTheme = (themeId) => {
    if (!themes.find(t => t.id === themeId)) return
    
    currentTheme.value = themeId
    document.documentElement.setAttribute('data-theme', themeId)
    localStorage.setItem('theme', themeId)
  }
  
  // 获取当前主题信息
  const currentThemeInfo = computed(() => {
    return themes.find(t => t.id === currentTheme.value) || themes[0]
  })
  
  // 随机主题
  const randomTheme = () => {
    const randomIndex = Math.floor(Math.random() * themes.length)
    setTheme(themes[randomIndex].id)
  }
  
  // 初始化主题
  const initTheme = () => {
    const savedTheme = localStorage.getItem('theme')
    const systemPrefersDark = window.matchMedia('(prefers-color-scheme: dark)').matches
    
    if (savedTheme && themes.find(t => t.id === savedTheme)) {
      setTheme(savedTheme)
    } else if (systemPrefersDark) {
      setTheme('dark')
    } else {
      setTheme('light')
    }
  }
  
  // 监听系统主题变化
  const watchSystemTheme = () => {
    const mediaQuery = window.matchMedia('(prefers-color-scheme: dark)')
    const handler = (e) => {
      if (!localStorage.getItem('theme')) {
        setTheme(e.matches ? 'dark' : 'light')
      }
    }
    mediaQuery.addEventListener('change', handler)
    return () => mediaQuery.removeEventListener('change', handler)
  }
  
  onMounted(() => {
    initTheme()
    watchSystemTheme()
  })
  
  return {
    currentTheme,
    currentThemeInfo,
    themes,
    setTheme,
    randomTheme,
    initTheme,
  }
}
```

## 方式4：创建完整的主题切换面板

**`src/components/ThemePanel.vue`**

```vue
<template>
  <div class="fixed bottom-4 right-4 z-50">
    <div class="dropdown dropdown-top dropdown-end">
      <div tabindex="0" role="button" class="btn btn-primary btn-circle shadow-lg">
        <Icon :icon="PaletteIcon" theme="outline" size="24" />
      </div>
      <ul tabindex="0" class="dropdown-content z-[1] menu p-2 shadow-2xl bg-base-100 rounded-box w-64 gap-2">
        <li v-for="theme in themes" :key="theme.id" class="p-1">
          <button 
            @click="setTheme(theme.id)"
            class="flex items-center gap-3 p-2 rounded-lg transition-all"
            :class="{ 'bg-primary/10 text-primary': currentTheme === theme.id }"
          >
            <div 
              class="w-8 h-8 rounded-full shadow-md" 
              :style="{ backgroundColor: theme.color }"
            ></div>
            <div class="flex-1 text-left">
              <div class="font-medium">{{ theme.name }}</div>
              <div class="text-xs opacity-70">{{ theme.description }}</div>
            </div>
            <Icon 
              v-if="currentTheme === theme.id"
              :icon="CheckOneIcon"
              theme="outline"
              size="18"
              class="text-primary"
            />
          </button>
        </li>
        <div class="divider my-1"></div>
        <li>
          <button @click="randomTheme" class="btn btn-ghost btn-sm justify-center gap-2">
            <Icon :icon="ShuffleIcon" theme="outline" size="16" />
            随机主题
          </button>
        </li>
      </ul>
    </div>
  </div>
</template>

<script setup>
import { Palette, CheckOne, Shuffle } from '@icon-park/vue-next'
import { useTheme } from '@/composables/useTheme'

const PaletteIcon = Palette
const CheckOneIcon = CheckOne
const ShuffleIcon = Shuffle

const { currentTheme, themes, setTheme, randomTheme } = useTheme()
</script>
```

## 方式5：在代码中动态切换

```javascript
// 在任何组件中切换主题
import { useTheme } from '@/composables/useTheme'

const { setTheme } = useTheme()

// 切换到少女粉主题
setTheme('pink')

// 切换到高级黑主题
setTheme('dark')

// 随机切换
const randomTheme = () => {
  const themes = ['light', 'dark', 'pink', 'green', 'blue']
  const random = themes[Math.floor(Math.random() * themes.length)]
  setTheme(random)
}
```

## 方式6：通过 URL 参数切换

**`src/utils/theme.js`**

```javascript
// 从 URL 参数获取主题
export const getThemeFromUrl = () => {
  const params = new URLSearchParams(window.location.search)
  const theme = params.get('theme')
  const validThemes = ['light', 'dark', 'pink', 'green', 'blue']
  return validThemes.includes(theme) ? theme : null
}

// 在 main.js 或 App.vue 中使用
const urlTheme = getThemeFromUrl()
if (urlTheme) {
  document.documentElement.setAttribute('data-theme', urlTheme)
  localStorage.setItem('theme', urlTheme)
}
```

## 使用示例

### 在 App.vue 中添加主题面板

```vue
<template>
  <div class="min-h-screen bg-base-200">
    <nav class="navbar bg-base-100 shadow-lg">
      <!-- 导航栏内容 -->
    </nav>
    
    <main>
      <router-view />
    </main>
    
    <!-- 添加主题切换面板 -->
    <ThemePanel />
  </div>
</template>

<script setup>
import ThemePanel from '@/components/ThemePanel.vue'
</script>
```

### 在组件中使用

```vue
<template>
  <div class="p-4">
    <h2>当前主题：{{ currentThemeInfo.name }}</h2>
    <button @click="setTheme('pink')" class="btn btn-primary">
      切换少女粉
    </button>
  </div>
</template>

<script setup>
import { useTheme } from '@/composables/useTheme'

const { currentThemeInfo, setTheme } = useTheme()
</script>
```

## 总结

你的项目已经配置好了主题切换功能：

1. ✅ **已创建 ThemeSwitcher 组件** - 在右上角显示主题切换按钮
2. ✅ **已配置5个主题** - 简约白、高级黑、少女粉、清新绿、海洋蓝
3. ✅ **支持持久化** - 主题选择会保存在 localStorage
4. ✅ **自动跟随系统** - 可以监听系统主题变化

现在你只需要在 `App.vue` 中引入 `ThemeSwitcher` 组件，或者在页面右下角添加 `ThemePanel` 组件，就可以看到漂亮的主题切换效果了！